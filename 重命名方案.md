# ABD9 音乐文件重命名方案

> 版本：v1.0（待实现）
> 状态：方案已确认，等待「开始改代码」指令

## 背景

现有音乐库 `D:/音乐/01-music/` 共 **12724** 个文件，经统计约 **1997 个（15.7%）** 文件名不符合统一命名规范，需批量整理。

| 问题类别 | 数量 | 占比 |
|---------|------|------|
| 多歌手非 `&` 分隔（`,`、`、`、`_`、`+`） | 1372 | 10.8% |
| 版本词大小写/词形不统一（live/Live/Live版、DJ/dj） | 783 | 6.2% |
| 全角括号 `（）` | 481 | 3.8% |
| 多空格 / 全角空格 | 206 | 1.6% |
| 繁体 / 异体字 | 46 | 0.4% |
| 括号未闭合 | 37 | 0.3% |

> 注：以上类别有重叠，一个文件可能同时存在多项问题。

---

## 一、目标命名规则

```
歌手 - 歌曲名(版本)
```

| # | 规则 | 说明 | 示例 |
|---|------|------|------|
| 1 | `歌手 - 歌名` | 保留现有分隔，所有文件已含 ` - ` | 不动 |
| 2 | 多歌手用 ` & ` 分隔 | 两侧带空格 | `林俊杰 & 张韶涵` |
| 3 | 版本用半角 `()` 包覆 | 统一半角括号 | `(Live)` |
| 4 | 简体中文 | 繁体/异体字转简体 | `女人何苦为难女人` |
| 5 | 单半角空格 | 多空格/全角空格合并 | `George Winston - Variations...` |

---

## 二、判定与转换规则（按顺序执行）

### 规则 1：多歌手分隔符统一

将歌手部分（` - ` 之前）中的以下分隔符统一为 ` & `（两侧带空格）：

| 原分隔符 | 示例 | 转换后 |
|---------|------|--------|
| `,`（半角逗号） | `李克勤, 容祖儿` | `李克勤 & 容祖儿` |
| `，`（全角逗号） | `彭羚，王力宏` | `彭羚 & 王力宏` |
| `、`（顿号） | `Ayo97、周思涵` | `Ayo97 & 周思涵` |
| `_`（下划线） | `海鸣威 _ 吴琼` | `海鸣威 & 吴琼` |
| `+`（加号） | `A + B` | `A & B` |
| 多空格（无分隔符） | `海鸣威  吴琼` | `海鸣威 & 吴琼` |
| `&` 无空格 | `Ayo97&周思涵` | `Ayo97 & 周思涵` |

**边界情况**：
- 已是 `A & B` 格式（如 `伍佰 & China Blue`、`孙建平 & Sweet Style`）→ 不动
- 单空格连接的歌手名（如 `George Winston  - ...` 中分隔符前的单空格）→ 不视为多歌手，仅合并多余空格
- 多空格转换只针对**歌手名内部**的多空格，不针对 `歌手  -  歌名` 分隔符附近的多空格（分隔符附近由规则 6 处理）

### 规则 2：括号统一为半角

- 全角 `（` `）` → 半角 `(` `)`
- 括号未闭合（37 个）→ 自动补全右括号 `)`
  - 示例：`Someone Like You (Live Acousti.mp3` → `Someone Like You (Live Acousti).mp3`
  - ⚠ 内容本身截断的文件，补全后仍可能不完整，重命名时列入但**标记提示**

### 规则 3：版本词统一

**主规则（映射表）**：

| 原写法 | 统一为 |
|--------|--------|
| `live`、`Live版`、`Live` | `(Live)` |
| `dj`、`DJ版`、`DJ 车载版`、`DJ完整版`、`dj版` | `(DJ版)` |
| `remix`、`Remix`、`Remix版` | `(Remix)` |
| `phonk`、`PHONK.Pt1`、`PHONK` | `(PHONK)` |
| `slowed`、`Slowed`、`Super Slowed` | `(Slowed)` |
| `explicit` | `(Explicit)` |
| `acoustic` | `(Acoustic)` |
| `instrumental` | `(Instrumental)` |
| `original mix` | `(Original Mix)` |
| `radio edit` | `(Radio Edit)` |
| `album version` | `(Album Version)` |
| `demo` | `(Demo)` |
| `sped up` | `(Sped Up)` |

**通用规则**：
- 中文版本词（伴奏 / 钢琴 / 女声版 / 粤语版 / 国语版 / 现场版 / 独唱版 / 合唱版 / 完整版 等）**保留原词**，仅规范括号
- 未列入映射表的英文版本词 → 保留原词，仅统一首字母大写
- 备注类括号内容（电影插曲 / cover 原唱 / 翻自 / 哈基米 等）**全部保留**，仅统一括号格式

### 规则 4：繁体 → 简体

- 使用 **opencc**（OpenCC 开放中文转换）完整转换
- 范围：歌手名 + 歌名 + 版本词中的繁体字
- 示例：`辛曉琪 - 女人何苦為難女人` → `辛晓琪 - 女人何苦为难女人`

### 规则 5：空格规范

- 多个连续半角空格 → 单个半角空格
- 全角空格 `　` → 半角空格
- 示例：`George Winston  -  Variations On The Canon By Pachelbel` → `George Winston - Variations On The Canon By Pachelbel`

### 规则 6：歌名内 `-` 保留

- 歌名（` - ` 之后）内部的 `-` 作为副标题**保留不动**
- 示例：`Georges Delerue - A LITTLE ROMANCE - Main Title` 保持原样
- 例外：歌手名内部的 `-`（如 `7YE - YOSHO...` 中歌手无 `-`；`Banda AK-47`）保留

### 规则 7：非法字符防护

重命名时过滤/拒绝以下内容：
- Windows 非法字符：`\ / : * ? " < > |`
- 首尾空白字符
- 尾部句点 `.`
- Windows 保留文件名：`CON`、`PRN`、`AUX`、`NUL`、`COM1-9`、`LPT1-9`（含带扩展名）
- 空文件名（转换后为空 → 跳过并提示）

---

## 三、功能设计

### 3.1 概览区改动

- 「扫描结果」概览区：「总文件数」→「**待重命名**」
- 显示数字 = 当前任务中文件名不符合规范的文件数
- **统计时机**：扫描完成后自动统计
- **点击行为**：弹出「重命名管理」窗口（其他概览项点击仍切换视图）

### 3.2 重命名管理弹窗

**「重命名」页**：
- 列表展示：`旧文件名 → 新文件名` 预览
- 默认全选，可勾选/取消
- 点击「重命名」→ 只处理勾选项
- 重命名失败（占用/权限/目标冲突等）→ 单独提示

**「恢复」页**：
- 列表展示已重命名的文件：`新文件名 → 旧文件名`
- 可勾选恢复部分或全部
- 恢复后从日志中移除

### 3.3 重命名日志

- 存储位置：exe 同目录 `rename_log.json`
- 格式：

```json
{
  "entries": [
    {
      "old_path": "D:/音乐/01-music/李克勤, 容祖儿 - 世界真细小.mp3",
      "new_path": "D:/音乐/01-music/李克勤 & 容祖儿 - 世界真细小.mp3",
      "renamed_at": "2026-08-06 16:30:00"
    }
  ]
}
```

### 3.4 重名冲突处理

- 目标文件名已存在 → **自动加后缀** `(1)`、`(2)`…
  - 示例：`王菲 - 红豆(1).mp3`
- 加后缀仍冲突 → 继续递增直到可用

### 3.5 重命名完成后

- **自动重新扫描**当前任务
- 重新扫描后「待重命名」数量更新

---

## 四、技术要点

| 项 | 方案 |
|----|------|
| 繁简转换 | opencc 库（打包体积 +~2MB） |
| 版本词表 | 内置映射表（规则 3 主规则 + 通用词） |
| 重命名执行 | `os.rename`，逐个执行，失败收集提示 |
| 日志持久化 | JSON 文件，原子写入 |
| 扫描统计 | 复用现有扫描结果文件列表，正则判定 |
| PyInstaller | spec 增加 opencc 相关 hiddenimports（如需要） |

---

## 五、待确认/风险

1. **括号内容截断**（37 个）：自动补 `)` 后内容仍可能不完整（如 `(Live Acousti`），重命名后文件内容本身无变化，仅文件名补齐——是否接受？若否，可改为标记⚠跳过
2. **opencc 打包**：需验证 PyInstaller 下 opencc 的共享库（opencc.dll / 数据文件）能否正确打包，失败则回退内置映射表
3. **重命名后自动重扫**：重扫会使当前勾选/视图状态重置，属预期行为
