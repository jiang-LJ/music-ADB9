# 分析：将"快速操作"拆分独立成栏

## 当前布局（5列）
```
┌─────────┬──────────────┬─────────────────────┬─────────────────┬─────────────────┐
│任务管理  │  当前任务     │ 扫描结果(可点击)     │     操作         │ 软件使用说明    │
│         │ 文件夹A      │                     │ 🚀开始扫描       │                │
│[保存任务]│ 文件夹B      │ ●总文件数 4876      │ ♻️移到回收站    │ [软件使用说明]  │
│[加载任务]│              │ ●重复文件 0         │ ↩️撤销移动      │ 联络作者微信    │
│...      │              │ ●相似文件 28        │ ├快速操作        │ a_better_day_9 │
│         │              │ ...                 │ │选A重复 选B重复 │                │
│         │              │                     │ │相似去重 近似去重│                │
│         │              │                     │ │取消选择         │                │
└─────────┴──────────────┴─────────────────────┴─────────────────┴─────────────────┘
```

## 新布局（6列）
```
┌─────────┬──────────────┬────────────┬─────────────────────┬──────────────┬─────────────────┐
│任务管理  │  当前任务     │    操作    │ 扫描结果(可点击)     │   快速操作    │ 软件使用说明    │
│         │ 文件夹A      │ 🚀开始扫描  │ ●总文件数 4876      │   选A重复    │ [软件使用说明]  │
│[保存任务]│ 文件夹B      │ ♻️移到回收站│ ●重复文件 0         │   选B重复    │ 联络作者微信    │
│[加载任务]│              │ ↩️撤销移动  │ ●相似文件 28        │   相似去重   │ a_better_day_9 │
│...      │              │            │ ...                 │   近似去重   │                │
│         │              │            │                     │   取消选择   │                │
└─────────┴──────────────┴────────────┴─────────────────────┴──────────────┴─────────────────┘
```

## 改动内容

### 1. `setup_ui()` — 6列布局
- `columnconfigure` 增加第5列
- 列顺序调整：任务管理(0) | 当前任务(1) | 操作(2) | 扫描结果(3) | 快速操作(4) | 帮助(5)
- 扫描结果保持 `minsize=300` 不变
- 任务管理、软件使用说明宽度不变
- 当前任务保持 `weight=1`，自然适应（新增一列后会变窄）

### 2. `_fill_operation_panel()` — 移除快速操作
- 只保留上部：🚀开始扫描 / ♻️移动到回收站 / ↩️撤销移动
- 移除下部快速操作按钮和已选择统计

### 3. 新增 `_fill_quick_action_panel()` — 独立栏，竖列布局
```
┌─ 快速操作 ──┐
│  选A重复   │
│  选B重复   │
│  相似去重  │
│  近似去重  │
│  取消选择  │
│            │
│ 已选择: 0  │
└────────────┘
```
- 5个按钮垂直排列
- 已选择统计放在底部

### 4. 移除原 `_fill_quick_action_panel()` 方法（如果还有残留）

## 关于当前任务列
当前任务列是 `weight=1`，在6列布局下自然变窄来适应总宽度。路径输入框仍然可以左右滚动查看长路径，功能不受影响。

## 确认问题
> **快速操作按钮的宽度是否需要固定？** 例如固定 `width=10`，让按钮整齐统一。
