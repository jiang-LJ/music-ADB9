# ABD9音乐文件筛查 - 技术文档

> 版本：V16（已开发完成）  
> 文档状态：已完成  
> 最后更新：2026-07-16

---

## 1. 项目概述

### 1.1 项目背景
开发一款面向音乐文件管理的桌面工具，支持对两个音乐文件夹进行**重复检测**、**相似检测**、**增量扫描**和**任务记录**。工具采用绿色便携式设计，EXE 文件名为 `ABD9.exe`，窗口标题为 `ABD9音乐文件筛查器`。数据库存储在 EXE 同级目录下，将整个 `dist/ABD9` 文件夹复制到任意位置（包括 U 盘或其它电脑）即可直接运行，无需安装 Python 或任何额外依赖。

### 1.2 核心功能

| 功能模块 | 说明 |
|---------|------|
| **任务管理** | 创建/加载/删除任务，自动保存文件夹路径和扫描历史；支持 JSON 导入/导出；恢复默认提供「清空任务列表」和「软件恢复初始设置」两个选项；加载任务弹窗支持 Checkbutton 多选勾选与批量删除 |
| **增量扫描** | 基于文件大小+修改时间（或MD5哈希）检测变更，仅扫描新增/修改/移动的文件；音频时长与 MD5 均持久化缓存，未变更文件不再重复读取/计算 |
| **重复检测** | 优先按 MD5 匹配，无 MD5 时按「文件名（不区分大小写）+ 文件大小」匹配；MD5 持久化缓存，未变更文件复用缓存 |
| **相似检测** | 严格同名匹配：同名同后缀大小不同，或同名不同后缀（如 song.mp3 与 song.flac） |
| **近似检测** | 文件名主干模糊匹配（rapidfuzz）+ 时长值过滤，默认相似度 80%、时长值 98%；自动排除已归类为重复/相似的文件 |
| **单文件夹扫描** | 支持仅扫描文件夹A或仅扫描文件夹B |
| **变更追踪** | 标记新增(new)、修改(modified)、未变(unchanged)、删除(deleted)、移动(moved) |
| **扫描预估/进度** | 扫描前显示文件数量与耗时预估；扫描时弹出独立进度窗口，后台线程执行 |
| **结果勾选与高亮** | Treeview 行勾选后显示红色 ☑；点击行时对侧对应位置同步黄色高亮 |
| **文件操作** | 勾选文件后可移入应用内部回收站（`AppTrash/`），支持撤销恢复；敏感操作需验证作者微信号 |
| **会话自动保存** | 退出前自动保存当前任务 ID、路径和扫描选项到 `last_session.json`，下次启动自动恢复 |
| **音频时长读取** | 扫描时按需读取音频文件时长并缓存到数据库；未变更文件下次扫描直接复用缓存，大幅提升增量扫描速度 |
| **软件使用说明** | 内嵌富文本帮助窗口，含四种文件定义详解 |

### 1.3 四种文件定义

| 文件类型 | 定义 | 计算方式 |
|---------|------|---------|
| **重复文件** | 内容完全相同的文件。优先按 MD5 匹配，无 MD5 时按「文件名（不区分大小写）+ 文件大小」匹配 | 重复文件组中所有文件的总数 |
| **相似文件** | 文件名相同但内容可能不同的文件。同名同后缀大小不同，或同名不同后缀 | 相似文件组中所有文件的总数 |
| **近似文件** | 文件名主干模糊匹配（rapidfuzz）+ 时长值过滤，默认相似度 80%、时长值 98% | 近似文件组中所有文件的总数 |
| **唯一文件** | 不属于重复、相似、近似任何一类的文件 | 总文件数 − 重复文件 − 相似文件 − 近似文件 |

> 四者严格互斥，相加等于总文件数。

### 1.4 变更类型标记

- 🟢 **NEW** - 新增文件
- 🟡 **MODIFIED** - 修改文件（大小/时间变化）
- 🔵 **MOVED** - 移动文件（路径变化但MD5相同）
- 🔴 **DELETED** - 删除文件（上次存在，本次不存在）
- ⚪ **UNCHANGED** - 未变更（跳过内容级扫描）

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         任务记录与增量扫描架构                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐                │
│  │   任务管理器   │────▶│  状态数据库   │────▶│  增量扫描引擎 │                │
│  │  TaskManager │     │  SQLite/JSON │     │ Incremental  │                │
│  └──────┬───────┘     └──────────────┘     └──────┬───────┘                │
│         │                                          │                         │
│         │  1. 保存任务记录                          │  3. 对比文件状态         │
│         │  2. 加载历史任务                          │  4. 识别变更文件         │
│         │  3. 管理任务版本                          │  5. 仅扫描变更部分       │
│         ▼                                          ▼                         │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         扫描模式选择                                  │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐ │   │
│  │  │  全新扫描    │  │  增量扫描    │  │  继续任务    │  │  对比模式   │ │   │
│  │  │ Full Scan   │  │ Incremental │  │  Resume     │  │  Compare   │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 编程语言 | Python 3.12 | 开发环境版本；打包后目标系统无需安装 Python |
| GUI框架 | Tkinter | 标准库内置，PyInstaller 自动打包 Tcl/Tk 运行时 |
| 数据库 | SQLite3 | 标准库内置，启用 WAL 模式提升并发性能 |
| 哈希算法 | hashlib.md5 | 分块计算（8MB/块），优化大文件性能 |
| 打包工具 | PyInstaller 6.19.0 | 生成独立 EXE（OneDir 模式），64 位 Windows 可执行 |

---

## 4. 项目目录结构

```
music-screen/
├── main.py                      # 程序入口，启动Tkinter应用
├── task_manager.py              # 任务管理器：数据库操作、增量扫描核心
├── scanner_app.py               # Tkinter主界面：MusicScannerWithTasks类
├── scanner_core.py              # 扫描核心：重复/相似/近似检测算法
├── utils.py                     # 工具函数：compute_md5、get_app_dir、常量定义等
├── MusicScanner.spec            # PyInstaller 打包规格文件
├── icon.ico                     # EXE 图标（256×256，任务栏/标题栏/资源管理器均清晰）
├── tasks.db                     # SQLite数据库（运行时自动生成，位于 EXE 同级）
├── AppTrash/                    # 应用内部回收站（运行时自动生成）
├── last_session.json            # 上次会话状态（运行时自动生成）
├── test_phase1.py               # Phase 1 单元测试
├── test_integration.py          # 集成测试
└── 开发技术文档.md               # 本文档
```

---

## 5. 模块设计

### 5.1 `utils.py` - 公共工具模块

**职责：** 存放与业务逻辑无关的通用工具函数和常量。

**包含内容：**

```python
# 常量
TIME_TOLERANCE = 2.0            # 时间容差：2秒内视为相同
MD5_CHUNK_SIZE = 8 * 1024 * 1024  # MD5分块大小：8MB
BATCH_SIZE = 500                # SQLite批量插入批次大小

# 枚举
class ChangeStatus(Enum)
class ScanType(Enum)

# 数据类
@dataclass class FileState
@dataclass class TaskRecord

# 工具函数
def compute_md5(filepath: str) -> Optional[str]
def get_audio_duration(filepath: str) -> Optional[float]  # 优先 tinytag，fallback mutagen
```

**设计原则：**
- `compute_md5` 作为模块级函数，不放在 `TaskManager` 内部，避免违反单一职责原则。
- 兼容Python 3.7：MD5计算使用传统 `while True` 循环，不使用海象运算符。

### 5.2 `task_manager.py` - 任务管理器

**职责：** 任务CRUD、文件状态持久化、增量扫描检测、扫描历史记录。

**核心类：** `TaskManager`

| 方法 | 职责 |
|------|------|
| `__init__(db_path)` | 初始化数据库连接（默认存储在程序目录 `tasks.db`） |
| `_get_conn()` | 懒加载单一连接，启用WAL模式、NORMAL同步、5秒busy_timeout、外键约束 |
| `_init_database()` | 创建 `tasks`、`file_states`、`scan_history` 三张表及索引 |
| `create_task()` | 创建新任务，返回 `TaskRecord` |
| `get_task()` / `list_tasks()` / `update_task()` / `delete_task()` | 任务CRUD |
| `get_previous_state()` | 获取某任务某文件夹的上次文件状态 |
| `detect_changes()` | **核心方法**：对比当前文件与历史状态，输出变更列表和统计 |
| `save_file_states()` | 批量保存文件状态（分批插入，每批500条） |
| `record_scan_history()` | 记录一次扫描的历史统计 |
| `get_scan_history()` | 查询某任务的扫描历史 |
| `export_all_data()` | 导出所有任务、文件状态、扫描历史为 JSON |
| `import_all_data()` | 从 JSON 导入任务数据；为新任务重新生成 UUID，并自动跳过同名同路径的重复任务 |

**线程安全策略（V4/V8修复后）：**
- `TaskManager` 维护 `_conn_map: Dict[int, sqlite3.Connection]`，以 `threading.current_thread().ident` 为键
- 每个线程首次调用 `_get_conn()` 时独立创建连接，启用 WAL 模式 + `busy_timeout=5000`
- 彻底解决后台扫描线程与主 UI 线程同时访问 SQLite 导致的 `SQLite objects created in a thread can only be used in that same thread` 错误

**增量检测算法：**
1. 读取历史状态，构建 `{path: FileState}` 字典（含缓存的 `duration` 和 `md5_hash`）
2. 构建MD5倒排索引（可选，用于移动检测）
3. 遍历当前文件：判断新增、移动、修改、未变
4. 对 **UNCHANGED** / **MOVED** / **DELETED** 状态透传历史缓存的 `duration` 和 `md5_hash`
5. 遍历历史路径：判断删除（排除已认领的移动路径）
6. 返回变更列表和统计字典

**JSON 导入注意事项（V10 修复）：**
- `import_all_data()` 在导入时会为每个新任务重新生成 `task_id`（UUID 前 8 位），并建立 `old_id -> new_id` 的映射
- 若某任务因「同名同路径已存在」被跳过，**必须确保该任务不进入映射表**，否则其关联的 `file_states` 和 `scan_history` 会插入一个不存在的新 `task_id`，触发 SQLite `FOREIGN KEY constraint failed` 错误
- 修复方式：先执行 `SELECT 1` 查重，确认不重复后再分配 `new_id` 并插入 `tasks` 表；`valid_new_ids` 仅包含实际入库的任务 ID

### 5.3 `scanner_app.py` - GUI主界面

**V15/V16 扫描性能优化：**
- `scan_directory()` 不再立即读取音频时长或计算 MD5，仅收集文件元数据
- 新增 `_apply_cached_durations()` / `_apply_cached_md5()`：将历史缓存透传到当前文件字典
- 新增 `_read_durations()`：仅对新增/修改文件调用 `get_audio_duration()`
- 新增 `_compute_md5_parallel()`：使用 `ThreadPoolExecutor`（8 worker）并行计算新增/修改文件的 MD5
- `perform_scan()` 流程调整为：扫描目录 → 检测变更 → 应用缓存时长 → 按需读取新/修改文件时长 → 应用缓存 MD5 → 多线程计算新/修改文件 MD5 → 查找重复/相似/近似 → 保存结果
- V15 使**增量扫描无变化时耗时从约 17s 降至约 0.1s**（实测 157x 加速）
- V16 使**二次 FULL 扫描（无文件变化）MD5 阶段从约 0.46s 降至约 0.03s**（实测 14x 加速），且日常增量/全新扫描均只计算变更文件

**职责：** Tkinter界面搭建、用户交互、调用扫描逻辑、结果展示与文件操作。

**核心类：** `MusicScannerWithTasks(tk.Tk)`

| 方法 | 职责 |
|------|------|
| `__init__()` | 初始化窗口（最大化）、标题 `ABD9音乐文件筛查器`、空白图标、颜色配置、任务管理器、绑定关闭确认事件 |
| `setup_ui()` | 搭建 6 列顶部布局 + 底部统一双 Treeview 结果区 |
| `_fill_task_panel()` | 任务面板：新建/加载/导入导出/恢复默认、扫描选项勾选框、相似度阈值（80-100%，默认80%）、时长值（80-100%，默认98%） |
| `_fill_operation_panel()` | 操作面板：开始扫描、移动到回收站、撤销移动、清空回收站 |
| `_sync_task_paths()` | 扫描前将 UI 路径同步到 `current_task` 并持久化，避免路径框显示与真实扫描路径不一致 |
| `_fill_overview_panel()` | 扫描结果概览：6 项统计（总文件/重复/相似/近似/唯一/耗时），彩色圆点可点击切换视图；显示为文件个数而非组数 |
| `_fill_quick_action_panel()` | 快速选择面板（V14 独立成栏）：上区 2×2 按钮（选A重复/选B重复/相似去重/近似去重），下区并排（取消选择 + 统计） |
| `on_scan_option_changed()` | 扫描选项变更通用回调：累积警告信息，末尾调用 `_sync_mutex_options()` 兜底同步互斥状态 |
| `validate_scan_options()` / `get_effective_scan_config()` | 扫描前验证与配置解析（返回含 `similarity_threshold`、`duration_threshold` 等） |
| `create_new_task()` / `load_existing_task()` / `import_export_tasks()` | 新建/加载/导入导出任务 |
| `restore_defaults()` | 恢复默认对话框：提供「清空任务列表」和「软件恢复初始设置」两个选项，均需验证作者微信号 `a_better_day_9` |
| `start_scan_with_task()` | 启动扫描入口：同步任务路径 → 估算 → 预估弹窗 → 进度弹窗 → 后台扫描 |
| `show_estimate_dialog()` / `show_scan_progress_dialog()` | 扫描预估与进度窗口 |
| `perform_scan()` | **核心扫描流程**：同步任务路径 → 文件系统扫描 → 检测变更 → 应用缓存时长/按需读取 → 应用缓存 MD5/多线程计算 → 重复/相似查找 → 结果保存 → 历史记录 |
| `refresh_results()` / `refresh_bottom_trees()` | 刷新概览统计与底部 A/B 双树；结果按组左右对齐显示，空位补行 |
| `on_tree_click()` / `on_tree_right_click()` | 切换勾选（仅 `#0` 列有效）、右键菜单 |
| `_on_tree_select()` / `_on_tree_background_click()` | Treeview 行选中时对侧同步高亮（黄色）；点击空白处清除高亮 |
| `_update_item_tags()` / `_clear_all_peer_highlights()` | 统一 tag 操作 Helper，避免高亮/勾选残留 |
| `quick_select()` | 快速选择：一键选中所有 A 重复 / B 重复 / 取消选择 |
| `select_smallest_in_similar()` / `select_smallest_in_approximate()` | 相似/近似去重：保留每组最大/最新的文件，其余全部勾选（A+B 都有时优先保留 A 侧；同 size 时保留 A 侧最新） |
| `send_selected_to_trash()` / `undo_delete()` | 移入内部回收站 / 撤销恢复，敏感操作需验证微信号 |
| `show_help()` | 显示软件使用说明内嵌窗口 |
| `show_scan_result_dialog()` | 扫描完成后的自定义结果弹窗（350×240，固定比例，确定按钮右下角） |
| `_save_session()` / `_load_session()` | 退出时保存 / 启动时恢复会话状态（`last_session.json`） |
| `_on_closing()` | 关闭窗口前保存会话并弹出确认对话框 |
| `show_toast()` | 显示临时提示气泡 |

**扫描选项互斥与自动恢复机制（V14）：**

定义了三类选项关系，通过 `_on_option_toggled()` 处理用户交互时的自动恢复，同时 `on_scan_option_changed()` 末尾调用 `_sync_mutex_options()` 作为兜底同步：

1. **互斥组（`_mutex_map` + `_mutex_groups`）**：
   - `full_scan` ↔ `incremental`：二选一，勾选其一自动取消另一个；若都取消，自动恢复为 `full_scan`
   - `fast_mode` ↔ `compute_md5`：二选一，勾选其一自动取消另一个；若都取消，自动恢复为 `fast_mode`
   - `fast_mode` ↔ `detect_moved`：互斥，勾选快速模式自动关闭检测移动

2. **必选组（`_min_one_groups`）**：
   - `scan_folder_a` + `scan_folder_b`：必须至少选一个；若都取消，自动恢复为两者都勾选

3. **依赖组（`_requires_map`）**：
   - `detect_moved` 依赖 `compute_md5`：勾选检测移动会自动勾选计算MD5，并连带关闭快速模式

**兜底同步（`_sync_mutex_options()`）：**
- `on_scan_option_changed()` 在显示完警告后，始终调用 `_sync_mutex_options()` 强制同步互斥状态
- 确保任何绕过 `_on_option_toggled()` 直接修改选项值的路径（如会话加载、测试直接 set）最终状态一致
- 例如：若 `fast_mode` 和 `compute_md5` 同时为 True，强制关闭 `compute_md5`

**验证与生效配置：**
- `validate_scan_options()` 在扫描前强制拦截无效配置（未选文件夹、路径为空、扫描模式为空等）
- `get_effective_scan_config()` 解析最终生效配置，并对首次扫描（`scan_count == 0`）强制使用全新扫描

所有自动恢复操作都会通过黄色警告标签显示提示信息（如"ℹ️ 已自动恢复为全新扫描"）。

**首次扫描特殊处理：**
- 如果 `scan_count == 0` 且用户未勾选全新扫描，也强制使用全新扫描

**图标与任务栏显示规则（V10 修复）：**
- `MusicScanner.spec` 中将 `music.ico` 声明为 `datas=[('icon.ico', '.')]`，确保打包后随 EXE 分发
- 运行时通过 `sys._MEIPASS` 获取 `_internal` 目录中的 `icon.ico` 绝对路径，再调用 `iconbitmap(icon_path)`，避免相对路径在打包后失效
- `_remove_window_icon()` 仅清除 `ICON_BIG`（Alt+Tab 大图标），**保留 `ICON_SMALL`**，从而确保任务栏能正常显示 `icon.ico` 图标
- 窗口标题栏本身由 `iconbitmap` 设置，不显示 Tk 默认羽毛图标

**Treeview 数据绑定规则：**
- 每行数据以**完整文件路径**作为 `iid`（内部唯一标识），而非默认生成的随机 ID
- 勾选状态、高亮状态均绑定到该 `iid`
- `_get_selected_paths()` 和 `open_file_location()` 直接读取 `iid` 即可获得真实路径，彻底避免了早期通过「文件名反查」导致的同名文件误选问题（例如：A/B 两文件夹存在同名重复文件时，移动操作可能错误地只移动 B 文件夹文件）

**Treeview 视觉状态规则：**
- `checked` tag（红色前景 `#ef4444`）：勾选 `#0` 列后显示红色 ☑
- `peer_highlight` tag（黄色背景 `#fef08a`）：点击某行时，对侧树对应索引行变黄；切换选中或点击空白处自动清除
- 业务 tag（`dup` / `sim` / `new` / `modified` 等）：刷新结果时保留，不受勾选/高亮操作影响

### 5.4 `scanner_core.py` - 扫描核心算法

**职责：** 重复文件检测、相似文件检测、近似文件检测。

**已实现接口：**

```python
def find_duplicates(files_a: Dict, files_b: Dict) -> List[List[Tuple[str, dict]]]:
    """基于 MD5（若已计算）或文件名+大小 查找重复文件组"""
```
- 优先使用已计算的 MD5 分组
- MD5 缺失时回退到 `name + size` 分组
- 返回的每组包含 `(path, info)` 元组列表

```python
def find_similar(files_a, files_b) -> List[List[Tuple[str, dict]]]:
    """严格同名匹配查找相似文件组"""
```
- 同名同后缀但大小不同（如两个大小不同的 song.mp3）
- 同名不同后缀（如 song.mp3 与 song.flac）

```python
def find_approximate(files_a, files_b, excluded_paths=None, similar_paths=None, threshold=0.80, duration_threshold=None, progress_callback=None) -> List[List[Tuple[str, dict]]]:
    """基于文件名主干模糊匹配 + 时长值过滤查找近似文件组"""
```
- 使用 `rapidfuzz.fuzz.ratio` 比较文件名去掉后缀后的主干部分，相似度达到设定阈值（默认 80%）即初步归为一组
- 通过首字符分组 + 并查集聚类，降低全量 O(n²) 比较开销
- **时长值过滤**：若 `duration_threshold` 存在且两文件均有有效 `duration`，计算 `shorter / longer` 比例，小于阈值则排除（避免将时长差异过大的文件误认为近似）
- 仅当两个文件都有有效时长（`duration > 0`）时才进行时长过滤；任一缺失时长则跳过此过滤
- 自动排除已被归类为重复或相似的文件路径
- 扫描过程中调用 `progress_callback(current, total)` 实时更新进度弹窗

**检测优先级：** 重复 → 相似 → 近似，依次排除已捕获文件，确保三者互不重叠。

### 5.5 `main.py` - 程序入口

```python
from scanner_app import MusicScannerWithTasks

if __name__ == "__main__":
    app = MusicScannerWithTasks()
    app.mainloop()
```

---

## 6. 数据库设计

### 6.1 任务表 `tasks`

| 字段 | 类型 | 说明 |
|------|------|------|
| `task_id` | TEXT PRIMARY KEY | 任务唯一ID（UUID前8位） |
| `task_name` | TEXT NOT NULL | 用户自定义任务名称 |
| `folder_a` | TEXT NOT NULL | 文件夹A路径 |
| `folder_b` | TEXT NOT NULL | 文件夹B路径 |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 最后更新时间 |
| `scan_count` | INTEGER DEFAULT 0 | 扫描次数 |
| `total_files_a` | INTEGER DEFAULT 0 | A文件夹文件总数 |
| `total_files_b` | INTEGER DEFAULT 0 | B文件夹文件总数 |
| `status` | TEXT DEFAULT 'active' | 任务状态 |

### 6.2 文件状态表 `file_states`

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT | 自增主键 |
| `task_id` | TEXT NOT NULL | 关联任务ID |
| `file_path` | TEXT NOT NULL | 文件完整路径 |
| `folder_type` | TEXT NOT NULL | A 或 B |
| `file_name` | TEXT NOT NULL | 文件名 |
| `file_size` | INTEGER NOT NULL | 文件大小（字节） |
| `modified_time` | REAL NOT NULL | 最后修改时间戳 |
| `md5_hash` | TEXT | MD5哈希值 |
| `duration` | REAL | 音频时长（秒），缓存 |
| `first_seen` | TIMESTAMP | 首次发现时间 |
| `last_scan` | TIMESTAMP | 最后扫描时间 |
| `change_status` | TEXT DEFAULT 'new' | 变更状态 |
| **约束** | FOREIGN KEY + UNIQUE(task_id, file_path) | 级联删除 + 路径唯一 |

### 6.3 扫描历史表 `scan_history`

| 字段 | 类型 | 说明 |
|------|------|------|
| `scan_id` | TEXT PRIMARY KEY | 扫描记录ID（UUID前12位） |
| `task_id` | TEXT NOT NULL | 关联任务ID |
| `scan_type` | TEXT NOT NULL | full / incremental / resume / compare |
| `scan_start` | TIMESTAMP | 开始时间 |
| `scan_end` | TIMESTAMP | 结束时间 |
| `duration_seconds` | REAL | 耗时（秒） |
| `files_scanned` | INTEGER | 扫描文件数 |
| `files_new` | INTEGER | 新增文件数 |
| `files_modified` | INTEGER | 修改文件数 |
| `files_deleted` | INTEGER | 删除文件数 |
| `duplicate_groups` | INTEGER | 重复组数 |
| `similar_groups` | INTEGER | 相似组数 |
| **约束** | FOREIGN KEY | ON DELETE CASCADE |

### 6.4 索引

```sql
CREATE INDEX idx_file_task ON file_states(task_id);
CREATE INDEX idx_file_path ON file_states(file_path);
CREATE INDEX idx_file_status ON file_states(change_status);
CREATE INDEX idx_history_task ON scan_history(task_id);
CREATE INDEX idx_file_md5 ON file_states(md5_hash);
```

---

## 7. UI界面设计

### 7.1 窗口规格
- 标题：`ABD9音乐文件筛查器`
- EXE 名称：`ABD9.exe`
- 图标：基于 `图标3.png` 生成（256×256 大尺寸，内容清晰占满画布，任务栏/标题栏/资源管理器均显示清晰）
- 尺寸：`1600x自动适配`（高度取 `min(屏幕高度-80, 980)`，默认最大化）
- 配色：中灰主题（背景 `#374151`，卡片 `#4b5563`，强调色 `#60a5fa`，文字 `#f3f4f6`）
- 空白图标：运行时用 Pillow 动态生成 1×1 透明 ICO，通过 Windows API 移除标题栏默认羽毛图标

### 7.2 界面分区

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  ABD9音乐文件筛查器                                                                      │
├────────┬────────────────────┬────────┬──────────────────┬──────────────┬────────────────┤
│ 任务管理│ 当前任务+文件夹A/B │  操作  │ 扫描结果(可点击) │  快速选择    │ 软件使用说明   │
├────────┴────────────────────┴────────┴──────────────────┴──────────────┴────────────────┤
│  底部统一结果区（左右 A/B 双栏，滚动条位于中间，橙黄色滑动块）                             │
│  ┌──────────────────────────────┐ ┌────┐ ┌──────────────────────────────┐                │
│  │ 文件夹 A                          │ │Scrollbar│ 文件夹 B                      │                │
│  │ 选择│序号│文件名│时长│大小│修改时间│ │      │ 选择│序号│文件名│时长│大小│修改时间│                │
│  └──────────────────────────────┘ └────┘ └──────────────────────────────┘                │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

**顶部 6 列布局（V14）：**
| 列 | 内容 | 宽度 |
|---|------|------|
| 0 | 任务管理 | 自适应 |
| 1 | 当前任务 + 文件夹A + 文件夹B | `weight=1` 动态适应 |
| 2 | 操作（开始扫描/移动到回收站/撤销移动/清空回收站） | `minsize=225` |
| 3 | 扫描结果概览（6项，可点击切换视图） | `minsize=300` |
| 4 | 快速选择（上2×2按钮+下并排：取消选择/统计） | `minsize=300` |
| 5 | 软件使用说明 | 自适应 |

**快速选择面板布局：**
```
┌─ 快速选择 ──┐
│ ┌───┬───┐  │
│ │选A│选B│  │
│ │重复│重复│ │
│ ├───┼───┤  │
│ │相似│近似│  │
│ │去重│去重│ │
│ └───┴───┘  │
│ ┌─────┬──┐ │
│ │取消 │统│ │
│ │选择 │计│ │
│ └─────┴──┘ │
└────────────┘
```
- 上区：4 个按钮 2×2 网格均分
- 下区：「取消选择」与「统计」并排
- 统计实时显示已选择总数及 A/B 侧数量

**底部双树列宽配置：**
- `#0`（选择）：12px，居中
- `no`（序号）：30px，居中
- `name`（文件名）：300px，居中
- `duration`（时长）：55px，居中，格式化为 `M:SS`，无效时显示 `--`
- `size`（大小）：35px，居中
- `mtime`（修改时间）：105px，居中

**结果行对齐规则：**
- 同一组重复/相似/近似文件按 A/B 侧拆分后，左右两侧逐行对齐显示
- 若某一侧文件数量较少，用空白行补齐，确保对侧高亮时行号严格对应

**滚动条样式与位置：**
- 滚动条位于 A/B 双树之间（`side=tk.LEFT` pack 顺序）
- 橙黄色滑动块（`#f59e0b`），悬停亮黄（`#fbbf24`），按下深橙（`#d97706`）
- 通过 `yscrollcommand` 回调实现 A/B 树的滚动位置实时同步

---

## 8. 开发方案与实施计划

### 8.1 开发阶段

| 阶段 | 状态 | 关键产出 |
|------|------|----------|
| **Phase 1** | ✅ 完成 | `utils.py`、`task_manager.py`、数据库测试通过 |
| **Phase 2** | ✅ 完成 | `scanner_app.py`、4列顶部+底部双树UI、帮助窗口 |
| **Phase 3** | ✅ 完成 | `perform_scan()` 完整跑通、扫描预估/进度弹窗、增量检测验证 |
| **Phase 4** | ✅ 完成 | `scanner_core.py`：重复/相似/近似检测算法 |
| **Phase 5** | ✅ 完成 | 结果双树展示、红色勾选、黄色对侧高亮、内部回收站/撤销/微信验证 |
| **Phase 6** | ✅ 完成 | 高亮逻辑严谨修复、图标定制、`ABD9.exe` 打包测试 |
| **V13** | ✅ 完成 | 时长列显示、默认值调整、快速选择面板交互优化 |
| **V14** | ✅ 完成 | 快速选择独立成栏、扫描选项互斥与自动恢复、时长值过滤 |
| **V15** | ✅ 完成 | 音频时长持久化缓存，增量扫描性能大幅提升 |
| **V16** | ✅ 完成 | MD5 持久化缓存 + 多线程并行计算，FULL/增量扫描均加速 |

### 8.2 关键实现总结

1. **`task_manager.py`**：以 `threading.current_thread().ident` 为键维护多连接池，彻底解决 Tkinter 主线程与后台扫描线程的 SQLite 并发访问问题；支持 JSON 全量导入/导出。
2. **`scanner_app.py`**：完成从骨架到完整产品的全部 UI 与交互逻辑，包括动态空白图标生成、高亮/勾选状态管理、微信验证对话框、扫描进度后台线程、内嵌帮助窗口。
3. **`scanner_core.py`**：实现了基于 MD5/名称+大小的重复检测、严格同名相似检测，以及基于 `rapidfuzz` + 首字符分组 + 并查集的近似检测，将大样本模糊匹配从 O(n²) 优化到可接受范围。
4. **V15/V16 性能优化**：将音频时长与 MD5 持久化到数据库，未变更/移动文件复用缓存，仅新增/修改文件重新读取/计算；MD5 计算使用 8 线程并行。实测 400 文件测试集下，首次全新扫描约 17s，增量无变化扫描降至约 0.1s，加速约 157 倍；200 文件测试集下，二次 FULL 扫描 MD5 阶段从 0.46s 降至 0.03s，加速约 14 倍。

### 8.3 风险与注意事项

| 风险点 | 应对方案 |
|--------|----------|
| Python版本兼容 | 开发环境使用 Python 3.12，但代码未使用 3.12 独占语法；打包后无需目标系统安装 Python |
| 大文件MD5计算卡顿 | 8MB 分块计算 + 后台扫描线程，UI 保持响应 |
| 音频时长读取成为扫描瓶颈 | **已修复（V15）**：时长持久化缓存，未变更文件复用缓存；首次扫描仍需读取，后续扫描大幅加速 |
| MD5 计算在文件量大时耗时 | **已修复（V16）**：MD5 持久化缓存 + 8 线程并行计算，仅新增/修改文件重新计算 |
| 选项冲突逻辑遗漏 | 严格按规则实现，并在 `validate_scan_options()` 中二次拦截 |
| SQLite并发写入 | **已修复**：采用 per-thread 连接池（`_conn_map`），而非单一连接 |
| 移动文件误判 | 仅在用户勾选"检测移动"且关闭"快速模式"时启用 |
| 相似检测大样本卡顿 | **已修复**：`find_similar` 改为严格同名匹配（O(n) 分组），`find_approximate` 使用 rapidfuzz + 首字符分组 |
| 对侧高亮残留 | **已修复**：`_on_tree_select` 始终先调用 `_clear_all_peer_highlights()`；空白处点击兜底清除 |
| 打包后图标缓存问题 | Windows 资源管理器可能缓存旧图标；复制到新路径或清 IconCache 可验证 |
| 音频时长读取依赖 | 优先 `tinytag`，失败时 fallback 到 `mutagen`；均失败则 `duration=None`，显示 `--`，近似检测忽略时长过滤 |
| 用户修改路径后任务仍使用旧路径 | **已修复**：扫描前调用 `_sync_task_paths()` 将 UI 路径写回 `current_task` 和数据库 |
| 本地 Tcl 不完整 | 开发机 Python Tcl 安装不完整，偶发 `_tkinter.TclError`，rerun 即可；不影响打包后的 EXE |

---

## 9. 交付确认

本项目已完成全部开发、测试与打包，交付物包括：

- **源码文件**：`utils.py`、`task_manager.py`、`scanner_app.py`、`scanner_core.py`、`main.py`
- **打包规格**：`MusicScanner.spec`
- **资源文件**：`icon.ico`（基于图标3.png生成）；空白图标运行时动态生成（`_generate_blank_ico()`）
- **可执行文件**：`dist/ABD9/ABD9.exe`（绿色便携，复制即运行；V16 重新打包，体积约 68MB）
- **测试覆盖**：`test_phase1.py` 全部通过；`test_integration.py` 全部通过（含 V15 新增的时长缓存测试 I-011 / I-012）

**已完成功能清单**：
- [x] 任务管理（保存/加载/导入/导出/恢复默认：含清空任务列表和软件恢复初始设置两个选项；加载弹窗支持多选勾选与批量删除）
- [x] 扫描选项与冲突自动处理
- [x] 扫描预估与后台进度弹窗
- [x] 增量扫描与变更追踪
- [x] 重复文件检测
- [x] 相似文件检测（严格同名匹配：同名同后缀大小不同 / 同名不同后缀）
- [x] 近似文件检测（文件名主干模糊匹配 + 时长值过滤，默认相似度80%、时长值98%）
- [x] 音频时长读取、显示与持久化缓存（Treeview 新增"时长"列，格式 M:SS；数据库缓存时长，增量扫描复用）
- [x] MD5 持久化缓存 + 多线程并行计算（V16）
- [x] 底部 A/B 双树结果展示
- [x] 红色勾选 + 黄色对侧高亮
- [x] 内部回收站 + 撤销恢复 + 清空回收站
- [x] 敏感操作微信验证
- [x] 软件使用说明内嵌窗口
- [x] 会话自动保存/恢复（`last_session.json`）
- [x] 自定义扫描结果弹窗（固定宽高比，按钮右下角）
- [x] 滚动条橙黄色样式 + 移至 A/B 之间
- [x] EXE 打包与图标定制

---

*文档生成时间：2026-04-18*  
*最后更新时间：2026-07-16*
